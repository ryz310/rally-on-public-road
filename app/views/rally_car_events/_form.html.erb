<%= form_for(@rally_car_event) do |f| %>
  <% if @rally_car_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rally_car_event.errors.count, "error") %> prohibited this rally_car_event from being saved:</h2>

      <ul>
      <% @rally_car_event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :beginning_on %><br>
    <%= f.date_select :beginning_on %>
  </div>
  <div class="field">
    <%= f.label :end_on %><br>
    <%= f.date_select :end_on %>
  </div>
  <div class="field">
    <%= f.label :note %><br>
    <%= f.text_area :note %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
